[build]
  command = "node build-config.js"
  publish = "."

[build.environment]
  GROQ_API_KEY = ""

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
